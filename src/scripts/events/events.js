const event = (events) => {
    return `
        <section class="event">
            <div>
                the event is ${events.type}
            </div>
        
        </section>
    
    `
}

export default event